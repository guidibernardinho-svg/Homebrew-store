jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkita64:latest   # or devkitarm, devkitppc, etc.
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: make  # your build command herejobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkita64:latest   # or devkitarm, devkitppc, etc.
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: make  # your build command here
